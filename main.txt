local table = {__index = function(_,w)
  w = math.floor(w)
  return string.sub(_[1], w,w)
end
}

local str = setmetatable({"tmp_str"},table)
print(str[1.1])--[w > 1].1
--lol

--inside lua ( print = nil), it's complicated



--my own pcall?


--An alternative in some cases is to read a compressed bytecode file for lua (a java lore file), etc.
--this is not associated with the scope

function icall(msg,func, ...)
  local _, err = load(func)
  if not _ then
    return msg
  end
  func(...)
  return "success"
end
